# KMS control plane — minimal dependencies
PyYAML>=6.0.1
Jinja2>=3.1.0
pypdf>=5.0.0

# PDF → Markdown (primary for chat exports / documents)
markitdown>=0.0.1
pymupdf4llm>=0.2.0

# Dev / tests
pytest>=8.0.0
