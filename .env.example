# Optional overrides (see kms/app/config.py)
# KMS_VAULT_ROOT=example-vault
# KMS_DATABASE_PATH=kms/data/state.db

# Ollama: optional external model directory (AnythingLLM + local stack)
# OLLAMA_MODELS=/Volumes/External/ollama-models
